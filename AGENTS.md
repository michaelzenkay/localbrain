# Agent Notes

This repository is intended to stay public, local-first, and free of private
history. Keep examples generic and avoid committing machine-specific paths,
real access keys, memory exports, logs, or personal notes.

Prefer small, testable changes. After changing the MCP function, bridge, or
schema, run the smoke test against a local Supabase/Ollama setup when possible.
